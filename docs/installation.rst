============
Installation
============

At the command line::

    $ easy_install django-rest-framework-choices

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-rest-framework-choices
    $ pip install django-rest-framework-choices
